#! /bin/bash

show_argument_info() {
    echo "-d  show bash debug info."
    echo "-b <node_name> set -bind -rpcbind address(default : target_addr(0.0.0.0) target_rpcaddr(127.0.0.1))"
    echo "-o <\"bincoin/bitcoin-cli arguments\"> \" must"
}
